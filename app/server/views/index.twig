{% extends 'layout.twig' %}

{% block content %}
	{% for project in projects %}
	    <section>
			<img src="{{asset('img/project')}}/{{project.name}}/index.jpg">
			<div>
				<h2>{{ project.title|raw}}</h2>
				<h3>{{ project.type|raw}}</h3>
				<p>{% for tag in project.tags %}<a href="#tag">{{tag}}</a>{% endfor %}</p>
				<a href="project/{{ project.name }}">olvass tovább</a>
			</div>
	    </section>
	{% endfor %}
{% endblock %}